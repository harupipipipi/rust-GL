#!/usr/bin/env bash
set -euo pipefail
# screenshot.sh — placeholder for automated demo-window screenshot capture.
#
# Future automation plan:
#   1. Launch the demo in the background with a virtual display (Xvfb on Linux).
#   2. Wait for the window to be rendered.
#   3. Capture the window with a tool like `scrot`, `maim`, or `screencapture` (macOS).
#   4. Save the image to docs/screenshot.png.
#   5. Optimise with `optipng` or `oxipng`.
#
# For now, follow the manual steps printed below.

echo "TODO: Launch demo, capture screenshot, save to docs/screenshot.png"
echo "Manual steps:"
echo "  1. cargo run"
echo "  2. Take screenshot of the window"
echo "  3. Save as docs/screenshot.png"
echo "  4. Update README.md to reference the image"
